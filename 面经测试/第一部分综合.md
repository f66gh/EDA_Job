下面我给你一份 **EDA/算法岗面向 C++ 基础的综合试卷**，覆盖你学习过的 **1.1 ~ 1.5 全部内容**。
 难度等同于真正的 EDA 实习/算法岗技术面，专门为你定制，确保覆盖：

- 内存模型
- 指针系统
- 引用系统
- const 系列
- 参数传递
- 生命周期、作用域
- 悬空指针、临时对象
- 指针数组/数组指针
- 左值、右值
- 常见陷阱

我只给题目，不给答案。
 你做完后我会给你逐题评分 & 讲透你不稳的地方。

------

# 🧪 **C++ 基础综合试卷（算法岗 / EDA 实习考核级）**

共 25 题（选择 + 判断 + 解释 + 代码走读 + 场景题）
 预计完成时间 60–90 分钟。

------

# 🟦 **Part A：选择题（单选/多选混合）**

### **A1. 下列哪些存储区域在程序结束后才会销毁？**

A. 栈
 B. 堆
 C. 静态区（global/static）
 D. 常量区（rodata）

------

### **A2. 下列哪段代码会导致未定义行为（UB）？**

A.

```cpp
int x = 10;
int* p = &x;
p = nullptr;
```

B.

```cpp
int* p = new int(10);
delete p;
delete p;
```

C.

```cpp
const char* s = "hi";
s = "world";
```

D.

```cpp
int x = 10;
int& r = x;
```

------

### **A3. 下面关于引用的叙述正确的是？**

A. 引用可以为 null
 B. 引用一旦绑定，不可重新绑定
 C. 引用底层一般用指针实现
 D. 引用没有生命周期问题

------

### **A4. 下面哪段代码返回的是“悬空引用”？**

A.

```cpp
int& foo() {
    static int x = 1;
    return x;
}
```

B.

```cpp
int& foo() {
    int x = 5;
    return x;
}
```

C.

```cpp
int& foo() {
    return *(new int(7));
}
```

D.

```cpp
int foo() {
    int x = 3;
    return x;
}
```

------

### **A5. 关于 const，哪项正确？（多选）**

A. `const int* p` → *p 不能改
 B. `int* const p` → p 不能改
 C. `const int& r = 10` → r 为临时变量绑定
 D. `int const* p` 等价于 `const int* p`

------

### **A6. 下列哪一个是“数组指针”？**

A. `int* arr[10];`
 B. `int (*arr)[10];`
 C. `int arr[10];`
 D. `int& arr[10];`

------

### **A7. 下面哪段代码会导致“悬空指针”？**

A.

```cpp
int x = 10;
int* p = &x;
```

B.

```cpp
int* p = new int(5);
```

C.

```cpp
int* foo() {
    int x = 10;
    return &x;
}
```

D.

```cpp
int* foo() {
    int* p = new int(7);
    return p;
}
```

------

### **A8. 下面表达式中，哪些是左值？**

A. `"hi"`
 B. `10`
 C. `a + 1`
 D. `a`
 E. `std::string("x")`

------

### **A9. 在 C++ 中，以下哪些函数参数传递方式会产生额外拷贝？**

A. `void foo(int x);`
 B. `void foo(int& x);`
 C. `void foo(const int& x);`
 D. `void foo(int* x);`

------

### **A10. 下面关于 `new` / `delete` 的描述正确的是：**

A. new 分配在堆，delete 自动设指针为 nullptr
 B. delete p 会析构 *p
 C. delete p 会删除指针变量 p
 D. delete nullptr 会崩溃

------

------

# 🟩 **Part B：判断 + 简答（理解题）**

### **B1. 判断 + 解释原因：**

```cpp
int* foo() {
    int x = 10;
    return &x;
}
```

是否 UB？为什么？

------

### **B2. 左值引用和 const 引用有什么区别？请各举一个例子。**

------

### **B3. 为什么 `"hi"` 是左值，而 `10` 是右值？**

------

### **B4. 指针本体（p）和指针指向对象（*p）分别存在哪里？为什么 delete 只删除 \*p？**

------

### **B5. 为什么 const 引用可以绑定临时量（比如 const int& r = 5）？内部发生了什么？**

------

### **B6. 请解释：**

```cpp
void foo(int*& p)
```

这是传什么？什么时候会用到？

------

### **B7. 为什么下面这段代码安全？**

```cpp
const std::string& r = std::string("abcd");
```

------

### **B8. 为什么不能返回局部变量的引用？请用“栈帧”解释一遍。**

------

------

# 🟨 **Part C：代码阅读题（核心考点）**

### **C1. 以下代码输出什么？说明原因。**

```cpp
int a = 1;

int& foo() {
    static int x = 10;
    return x;
}

int main() {
    int& r = foo();
    r = 20;
    cout << foo();
}
```

------

### **C2. 是否 UB？为什么？**

```cpp
int* p = new int(10);
int& r = *p;
delete p;
cout << r;
```

------

### **C3. 请问下面 a、b、c 分别是什么？**

```cpp
int arr[3] = {1,2,3};
int* a = arr;        // ?
int (*b)[3] = &arr;  // ?
int* c[3];           // ?
```

------

### **C4. 输出是什么？为什么？**

```cpp
void update(int x, int& y, int* z) {
    x++;
    y++;
    (*z)++;
}

int main() {
    int a = 1, b = 2, c = 3;
    update(a, b, &c);
    cout << a << b << c;
}
```

------

### **C5. 是否 UB？为什么？**

```cpp
int& bar() {
    return *(new int(100));
}

int main() {
    int& r = bar();
    delete &r;
    cout << r;
}
```

**但其实不会报错，只会 UB。**

解释：

```
int& bar() {
    return *(new int(100)); // 返回堆对象引用
}

int& r = bar();  // r 指向 heap 上 100
delete &r;       // 删除 *r → 合法
cout << r;       // ❌ r becomes dangling reference → UB
```

------

------

# 🟧 **Part D：场景大题（真正算法岗风格）**

### **D1. 画出下面程序运行时的内存布局（栈/堆/静态区/常量区）。**

```cpp
int g = 10;

int* foo(int x) {
    static int s = 20;
    int y = 30;
    int* p = new int(x + y + s);
    return p;
}

int main() {
    int a = 5;
    int* r = foo(a);
    return 0;
}
```

------

### **D2. 分析是否 UB，并说明每一步发生了什么：**

```cpp
int& foo() {
    int x = 100;
    return x;
}

int main() {
    int& r = foo();
    cout << r;
}
```

------

引用本质是“指向某对象的 const 指针”

### **D3. 下面的函数是否有 bug？为什么？**

```cpp
void push_back(int*& arr, int& size, int value) {
    int* newarr = new int[size + 1];
    for (int i = 0; i < size; i++)
        newarr[i] = arr[i];
    newarr[size] = value;

    delete[] arr; 
    arr = newarr;
    size++;
}
```

考点：指针引用、delete[] vs delete、是否会 UB。

------

# 🎯 做完我会按 EDA/算法岗标准给你逐题评分 + 找出薄弱点 + 讲透你还不稳的知识。

可以开始做了！