哈哈，好！那我也来一套 **“C++ const 教学水平终极检测卷（含陷阱）”**
 完全模仿你之前做的“指针 & 引用卷”的风格——
 **不写代码，只考你对 const 的理解能力。**

这些题都是 **面试官最喜欢拿来卡人、且最能体现是否真正理解 const 的题目**。

请你先做，我之后再给解析（doge）。
 准备好——开卷！

------

# 🧪 **【C++ const 深度理解测试卷】**

------

# **Part A：基础判断题（10 题）**

判断 **是否合法**，若不合法简要说明原因。

------

### **1.**

```cpp
int a = 10;
const int* p = &a;
*p = 20;    // ?
```

------

### **2.**

```cpp
int a = 10;
const int* p = &a;
p = nullptr;   // ?
```

------

### **3.**

```cpp
int a = 10;
int* const p = &a;
p = nullptr;   // ?
```

------

### **4.**

```cpp
int a = 10;
int* const p = &a;
*p = 30;       // ?
```

------

### **5.**

```cpp
int a = 10;
const int* const p = &a;
*p = 20;   // ?
```

------

### **6.**

```cpp
int a = 10;
const int* const p = &a;
int b = 20;
p = &b;    // ?
```

------

### **7.**

```cpp
int a = 10;
int b = 20;
const int* p = &a;
*p = b;     // ?
```

------

### **8.**

```cpp
int a = 10;
int b = 20;
const int* p = &a;
p = &b;     // ?
```

------

### **9.**

```cpp
int a = 10;
int b = 20;
int* p = &a;
const int* q = p;
*q = 30;    // ?
```

------

### **10.**

注意这道题只是不能通过q改

```cpp
int a = 10;
int* p = &a;
const int* const q = p;
*p = 40;       // ?
```

------

# **Part B：输出题（5 题）**

------

### **11.**

```cpp
int a = 10;
const int* p = &a;
cout << *p << endl;
```

------

### **12.**

```cpp
int a = 10;
int b = 20;
const int* p = &a;
p = &b;
cout << *p << endl;
```

------

### **13.**

```cpp
int a = 10;
int b = 20;
int* const p = &a;
*p = b; // 注意这是改值不是改指向
cout << a << endl;
```

------

### **14.**

```cpp
int a = 10;
int b = 20;
const int* const p = &a;
a = 99;
cout << *p << endl;
```

------

### **15.**

```cpp
int a = 10;
int* p = &a;
int* const q = p;
*p = 123;
cout << *q << endl;
```

------

# **Part C：理解力终极题（5 题）**

这是能完全区分“**真正懂 const**” vs “只会背”的关键题。

------

### **16.（核心）**

解释：

```cpp
const int* p;
```

p 能不能改？
 *p 能不能改？
 const 修饰谁？
 为什么？

------

### **17.（经典陷阱）**

解释：

```cpp
int* const p = &a;
```

p 能不能改？
 *p 能不能改？
 const 修饰谁？
 为什么？

------

### **18.（必会面试题）**

为什么以下两行含义不同？

```cpp
const int* p;
int const* p;
```

------

### **19.（最难的 const 题之一）**

解释为什么下面的代码是非法的：

```cpp
int** pp;
const int* p;
pp = &p;   // ？
```

提示：**const 能“升级”，不能“降级”**

```cpp
int\** 可以改 int
const int* 不能改 int
但你想让 int\** 指向 const int* → 会导致能改 const → 必须禁止
C++ 的 const 是“路径限定（path-sensitive）”，不是“对象限定（object-sensitive）”。
```

------

### **20.（思维题）**

一句话解释：

> 为什么 C++ 要把 “const 在 * 左边 / 右边” 设计得这么反直觉？

（看你理解深度）

```cpp
int* p;      // p 是 int* 类型
int* const p; // p 是 const 指针，类型是 int*
const int* p; // p 指向 const int
```

------

# 🎉 OK，试题全部给完了！

你先做，我之后给你逐题评分+解析，
 就像你之前 Part A 那样。

准备好了可以直接把答案发给我（doge）。