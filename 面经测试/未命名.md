1. map底层是红黑树，有序，时间复杂度O(logn)，unordered_map底层是哈希表，无需，时间复杂度O(n)；例如按时延排序门用map，统计每种门的个数用unordered_map（不需要排序）
2. 新建一个“clk”key，然后value是0，赋值给a，用迭代器寻找有没有key是"reset"的键值对，没有返回是mp.end()。执行完之后mp.size()是1；因为operator会为没有的key创建一个键值对，value默认是0，find是纯找
3. 键类型 值类型 如何把键转化为hash的方法 如何计算键相等的方法；默认为std::hash<string>,调用operator==吗？我也不知道怎么描述，底层是什么
4.  
